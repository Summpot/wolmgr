{
	"name": "wolmgr",
	"description": "Wake-on-LAN manager with a React frontend, Rust backend, and ESP32-S3 Rust broker.",
	"private": true,
	"packageManager": "pnpm@10.28.2",
	"scripts": {
		"check": "pnpm exec biome check --write . && cargo check -p wolmgr-backend",
		"dev:frontend": "pnpm --dir frontend dev",
		"dev:backend": "cargo run -p wolmgr-backend",
		"build:frontend": "pnpm --dir frontend build",
		"build:backend": "cargo build -p wolmgr-backend --release",
		"build": "pnpm -s build:frontend && pnpm -s build:backend"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.10"
	}
}
